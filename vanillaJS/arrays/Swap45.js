var arr1 = [5, 4, 2, 3, 8, 5, 9, 4, 1, 12, 15, 22];

// var arr = [...arr1,225, 335]
// document.write(arr);


// itterate through the array for 4 and get the index

// for (var i = 0; i < arr.length; i++) {
//   if (arr[i] == 4 && arr[i + 1] != 5) {
//     for (var j = 0; j < arr.length; j++) {
//       if (arr[j] == 5 && (j == 0 || arr[j - 1] != 4)) {
//         console.log(j, i);
//         var temp = arr[j];
//         arr[j] = arr[i + 1];
//         arr[i + 1] = temp;
//         console.log(arr);
//         break;
//       }
//     }
//   }
// }



// // itterate through the array for 5 and get the index

// }
// // swap index of 5 and next index of 4

// var temp = arr[index5];
// arr[index5] = arr[index4 + 1];
// arr[index4 + 1] = temp;

// // Iterate through the array to find the index of 4
// var index4 = -1;
// for (var i = 0; i < arr.length; i++) {
//     if (arr[i] === 4) {
//         index4 = i;
//         break;
//     }
// }

// // Iterate through the array to find the index of 5
// var index5 = -1;
// for (var j = 0; j < arr.length; j++) {
//     if (arr[j] === 5) {
//         index5 = j;
//         break;
//     }
// }

// // Swap the values
// if (index4 !== -1 && index5 !== -1) {
//     var temp = arr[index4 + 1];
//     arr[index4 + 1] = arr[index5];
//     arr[index5] = temp;
// }

// document.write(arr);




// functions
